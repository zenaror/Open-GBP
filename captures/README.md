# Captures

Hardware traces and captured test data.

Public, sanitized traces that are useful for automated regression tests
may be committed to this directory.

Local or private captures belong under:

- `captures/local/`
- `captures/private/`

Those directories are intentionally ignored by Git.

## Receiving a new physical run — rename BEFORE copy, never overwrite

The console names its files from the EMBEDDED test id and build id
(`GBP-VIDEO-004_stream-0009.log`, `…-idxcap.bin`, `…-disp.bin`). Two different
physical runs of the same binary therefore produce IDENTICAL names, and on
2026-09-20 run 8's drop overwrote run 7's raw copies in `logs/`; run 7 survived
only because a run-suffixed archive already existed here; on 2026-09-21 RUN 13's
files, moved from the SD card into `logs/` under the console's `stream-0013`
names, overwrote RUN 12's copies there, and RUN 12 survived the same way. This
must not depend on luck. Before a newly supplied run touches any local directory that may
already hold same-named files:

1. identify the intended **run number and experiment identity** (the embedded
   `TEST_ID`/build name does **not** mean the files belong to the same run);
2. reserve unique archive names in this directory's style —
   `<EXPERIMENT>_<build>-run<N>.log`, `…-run<N>-idxcap.bin`, `…-run<N>-disp.bin`,
   and, since `stream-0013`, `…-run<N>-full.bin` and `…-run<N>-vi.bin`
   (e.g. `GBP-VIDEO-004_stream-0010-run9.log`, `GBP-VIDEO-004_stream-0013-run12-full.bin`);
3. copy the supplied bytes under those names FIRST, with `cp --update=none`
   (refuse if the target exists), and `cmp` the copy against the supplied file;
4. hash the supplied bytes (`sha256sum`) before any interpretation; the
   executor's hash is the identity, the operator's media hash is a double check;
5. never overwrite, rewrite or delete an earlier raw physical artifact — not in
   `logs/`, not here;
6. keep the original generated name as metadata in the run's research record if
   it is useful.

`logs/` stays what `CLAUDE.md` §12 says it is: the raw drop, never edited and
never versioned. The preserved copy is the run-suffixed file here.

## fixtures/

| File | Origin | Status |
|------|--------|--------|
| `dolphin-2606a-gbplayer-model-present.gbpreplay` | Dolphin 2606a, `Core.HSPDevice=2`, probe build probe-0001 | **model data**, not hardware |
| `dolphin-2606a-gbplayer-model-absent.gbpreplay` | Dolphin 2606a, no HSP device | **model data**, not hardware |
| `hw-gamecube-nogbp-2026-09-14-probe-0001.gbpreplay` | **SOURCE = physical GameCube, GBP_PRESENT=no** (GBP-BASELINE-NOGBP-001), same DOL probe-0001, log sha256 `03e930ff…c8f9` kept under `captures/local/` | hardware capture, sanitized |
| `hw-gamecube-gbp-2026-09-15-init-0001.gbpreplay` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-INIT-001 build init-0001 commit a3d9668, log sha256 `d1e90daf…956e` (5497 B) | hardware capture: detection, PI, S0–S5, CONTROL writes |
| `hw-gamecube-nogbp-2026-09-15-init-0001.gbpreplay` | **SOURCE = physical GameCube, GBP_PRESENT=no**, same DOL, log sha256 `97f7cc70…6c5e` (2090 B) | hardware capture: `C1`×32, aborted before any CONTROL write |
| `hw-gamecube-gbp-2026-09-15-initirq-0001.gbpreplay` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-INIT-002 build initirq-0001 commit 4e3cb43, log sha256 `e7ec3d83…ea1d` (6585 B) | hardware capture: detection, PI, S0–S4, CONTROL writes, physical time base (`T`), interrupt path as it happened (`I i/u/m/r`; the handler never ran, so no IRQ is replayed) |
| `hw-gamecube-gbp-2026-09-15-initirqa-0001.gbpreplay` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-INIT-003A build initirqa-0001 commit d956b1b, DOL sha256 `8c225bd1…bfa5`, log sha256 `ae911745…2ef8` (13231 B) | hardware capture: detection, PI, BASE/P0, CONTROL transform, IRQ-register writes A1 `0x8AAE` / A2 `0x0000` / stop `0x8FAA` with every raw read, physical time base (`T`), the INTSR poll that saw bit 13 (`P p 00012000`), the single PI W1C (`P a`); PI HSP masked throughout (no `I` lines) |
| `hw-gamecube-gbp-2026-09-15-initirqb-0001.gbpreplay` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-INIT-003B build initirqb-0001 commit d3da8cd, DOL sha256 `821aa2b2…b757`, log sha256 `bedb1f01…cf7c` (17471 B) | hardware capture: the 003A sequence again (A1 `0x8AAE`, A2 `0x0000`, first cause `0x0400` at the PI 105.29 ms after A2), the handler installed after the latched cause (`I i null`), one unmask with the physical handler record (`I u …`: delivered with INTMR bit 13 = 1, masked inside the handler, one W1C, PI clear afterwards), the main re-mask (`I m`), device ACK `0x8500` read back `0x8000`, stop `0x8FAA`, handler restored (`I r`); no main-loop PI W1C; the source log's `pi_policy=never_unmasked` label is a documented defect of that build |
| `hw-gamecube-gbp-2026-09-16-initirq4-0001.gbpreplay` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-INIT-004 build initirq4-0001 commit 741630b, DOL sha256 `1da0d7b4…010c`, log sha256 `c9167224…775b` (19247 B) | hardware capture: the 003A sequence again (first cause `0x0400` 105.283 ms after A2, `0x0500` by PREUNMASK-0), the handler installed once (`I i null`), the generation published masked (`I p 0`), one unmask with the physical multi-cycle handler record (`I u …`: delivered with INTMR bit 13 = 1, masked inside, one W1C, PI clear 142 ticks later; latency 89 ticks), the main re-mask (`I m`), device ACK `0x8500` and — 26.0 µs later — POSTACK-0 `0x8400` (source 0x0400 present under bit 15 = 1, CONTROL 0x8C, PI bit 13 clear): the conservative clean boundary ended the run (`anomaly_source_not_cleared`), **no re-arm was written** (4 IRQ writes: A1, A2, ACK, stop `0x8EAA`), handler restored (`I r`); the multi-cycle continuation stays SYNTHETIC |
| `hw-gamecube-gbp-2026-09-16-avsvc-0001.gbpreplay` + `hw-gamecube-gbp-2026-09-16-avsvc-0001-blocks.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-AV-SERVICE-001 build avsvc-0001 commit d3a6d23, DOL sha256 `d9e6dccd…56ff`, log sha256 `d0324b6d…3713` (23154 B), sidecar sha256 `1c17a2d7…dc1e` (8204 B, format 2, byte-identical copy of the console's file) | hardware capture: the 003A sequence again (first cause `0x0400` 105.289 ms after A2, `0x0500` at PREUNMASK), one delivery through the 003B extended one-shot (`I u`, 72 ticks), PRESVC `0x0500`, AUDIO 0x1000 and VIDEO 0xF00 read by one whole-block DMA each (`B` lines with CRC-32 `fec5e4e7` / `fe45ff08`; bytes in the sidecar), POSTDRAIN `0x0500`, ACK `0x8500`, POSTACK `0x8000`, no main W1C, RE-ARM `0x0000`, REARMPOST `0x0400` with PI bit 13 = 1 (outcome B) 43.9 µs later, next cause never delivered, teardown S4B (stop `0x8FAA` → `0x8AAA`, one `P a`), physical time base; 132 operations replay with 0 mismatches / 0 exhausted / 0 blocks missing / 0 CRC mismatches to `ok_service_rearm_cause_observed`; the same script without the sidecar reports both blocks missing (exit 1) |
| `hw-gamecube-gbp-2026-09-16-video-0001.gbpreplay` + `hw-gamecube-gbp-2026-09-16-video-0001-seq.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-VIDEO-001 build video-0001 commit 6930dde, DOL sha256 `856d3e91…fd65`, log sha256 `ec3c366c…6527` (270580 B), sidecar sha256 `ce5134ff…e229` (403948 B, `OGBPSEQ1` v1, byte-identical copy of the console's file) | hardware capture: 209 cycles of the repeated drained service through ONE installed 003B one-shot (209 unmasks / 209 ISR entries / 209 ACK / 209 re-arms / 0 reentry / 0 main W1C / 1 teardown W1C), 88 VIDEO 0xF00 and 144 AUDIO 0x1000 whole-block reads all completed, sources 121×0400 / 65×0100 / 23×0500, frame starts at 0, 25, 65 with both predicates agreeing on all 88 blocks, one complete 40-block interval (seq25→seq65, 16.794 ms, 59.547 Hz), target reached with the 210th cause latched and closed by the teardown; status `ok_video_sequence_capture`, restore ok, errors 0. **Only 9 of the 144 AUDIO payloads were preserved by design** (first 8 + last valid): the other 135 `B` lines carry address, length, rc and timing but no bytes and **no CRC**, and the replay reports them as missing blocks. A `B` line carries a CRC-32 only where one was measured: 88/88 VIDEO, 9/144 AUDIO |
| `hw-gamecube-gbp-2026-09-16-vstate-0001.gbpreplay` + `hw-gamecube-gbp-2026-09-16-vstate-0001-vstate.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-VIDEO-002 build vstate-0001 commit e8f3a69, DOL sha256 `c73d49fa…19b9`, log sha256 `4f30d1cd…576c` (81380 B), sidecar sha256 `6406f244…2639` (2432396 B, `OGBPSEQ1` **v2**, byte-identical copy of the console's file) | hardware capture: 51751 admitted cycles over 8.186 s through ONE installed 003B one-shot; **the script is only the prefix the log records** (003A stage, the four verify cycles, teardown) because the other 51746 cycles are lean by design and kept no per-delivery record. The sidecar is the primary evidence: 489 frame signature records, 209 events, 4 episode descriptors, 81 sampled cycles, 15 preserved raw frames. The settled 274-frame state matches GBI reference table B in all forty blocks. **SERVICE ABORTED at cycle 51750 on a semantic disagreement whose 32 bytes this format could not preserve** — the header says so explicitly and they are not reconstructed |
| `hw-gamecube-gbp-2026-09-17-vstate-0002.gbpreplay` + `hw-gamecube-gbp-2026-09-17-vstate-0002-vstate.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-VIDEO-002 build vstate-0002 commit 8cbb28d, DOL sha256 `8661e914…b91b`, log sha256 `fb127d79…de2a` (40013 B), sidecar sha256 `f2ed596e…8c91` (12588 B, `OGBPSEQ1` **v3**, byte-identical copy of the console's file) | hardware capture: 518 cycles over 0.0842 s, same prefix-only script rule (513 lean cycles are not in it). **The first physical file of the family carrying a semantic-disagreement diagnostic**: the 96-byte record at `off_diag` 0x10C0 holds the 32 raw bytes of the read that aborted the run (`0101010001010100…05050500`), seven replicas at 0x0100 and the eighth at 0x0500, Disc 0x0500 against GBI 0x0100, differing by exactly the AUDIO source bit 0x0400. Header CRC `bd2a5f27`, total CRC `08514baa`. No structured state was observed and none is claimed: the abort came 0.42 s before the change vstate-0001 recorded |
| `hw-gamecube-gbp-2026-09-17-vstate-0003.gbpreplay` + `hw-gamecube-gbp-2026-09-17-vstate-0003-vstate.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-VIDEO-002-R3 build vstate-0003 commit 8c25df2, DOL sha256 `baea30f5…b486`, log sha256 `9f81f19f…2e57` (86378 B), sidecar sha256 `0a45d487…e6bc` (4359724 B, `OGBPSEQ1` **v4**, byte-identical copy of the console's file) | hardware capture: **the first run of this test to reach its scientific target** — 1 114 007 admitted cycles over 175.848 s, of which **120.009 s of valid post-baseline observation** against a 120 s target; `stop=nominal_negative`, `status=ok_structured_change_observed`, SERVICE ok, RESTORE ok, 0 errors. Same prefix-only script rule as vstate-0001/0002 (1 114 003 lean cycles are not in it); the sidecar is the evidence: 10 503 frame records, 210 events, 4 episode descriptors, 80 sampled cycles, 15 preserved raw frames, 2 AUDIO blocks and **23 diagnostics**. **Twenty-three semantic disagreements, all survived**: every one recomputes from its own 32 preserved bytes to Disc 0x0500 / GBI 0x0100 / delta 0x0400, all `SOURCE_SERVICED`, and the omitted AUDIO source returned in the next ordinary read 23 of 23 times. **KNOWN PRODUCER DEFECT (GBP-HW-104)** — the current-cycle fields (`authoritative_value`, `service_selected`, `ack_value`, `t_ack`, `t_rearm`) may belong to a **later** cycle than the record they sit in; the header NOTEs of the fixture carry the exact trusted/untrusted field split (GBP-HW-105) and nothing was repaired or reconstructed. Both CRCs verify (`888afeb1` / `df719b16`) and every section is contiguous: the defect is producer attribution, not storage |
| `hw-gamecube-gbp-2026-09-17-vstate-0004.gbpreplay` + `hw-gamecube-gbp-2026-09-17-vstate-0004-vstate.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-VIDEO-002-R4 build vstate-0004 commit b017e38, DOL sha256 `b0ed33f0…97c5`, log sha256 `e8d9e2dd…c1a4` (86390 B), sidecar sha256 `9d744a28…aaa1` (4360684 B, `OGBPSEQ1` **v5**, byte-identical copy of the console's file) | hardware capture: **the run that completed R3's physical validation** — 1 114 005 admitted cycles over 175.848 s, of which **120.009 s of valid post-baseline observation**; `stop=nominal_negative`, `status=ok_structured_change_observed`, SERVICE ok, RESTORE ok, transport_ok=1, 0 errors, 0 timeouts, 0 busy, 0 uncertain, 0 overflows. Same prefix-only script rule as the other vstate runs (1 114 001 lean cycles are not in it); the sidecar is the evidence: 10 503 frame records, 210 events, 4 episode descriptors, 80 sampled cycles, 15 preserved raw frames, 2 AUDIO blocks and **29 diagnostics**. **Twenty-nine semantic disagreements, all survived, and this time the diagnostics are trustworthy**: every record recomputes from its own 32 bytes to Disc 0x0500 / GBI 0x0100 / delta 0x0400 and carries `authoritative=0100`, `service_selected=0100`, `ack=8100`, `record_flags=01c1` and `t <= t_ack <= t_rearm <= t_next_cause` — **29 of 29, zero cross-field invariant failures, zero producer warnings**. The vstate-0003 producer defect (GBP-HW-104) does not occur once. Replica structure: contiguous `0x0500` suffix in 29 of 29, lengths 24/2/3. Both CRCs verify (`5baa1a83` / `1e16aee5`) and every section is contiguous |
| `hw-gamecube-gbp-2026-09-18-vstate-prewait-5000.gbpreplay` + `hw-gamecube-gbp-2026-09-18-vstate-prewait-5000-vstate.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, PRE-HANDLER MASKED WAIT diagnostic, build vstate-prewait-5000 commit 500429a, DOL sha256 `b5f0060a…8709`, log sha256 `20b5e2a7…555d` (45943 B), sidecar sha256 `79a44f82…9a28` (2024204 B, `OGBPSEQ1` **v5**, byte-identical copy of the console's file) | hardware capture, and **NOT a GBP-VIDEO-002 run that reached its target**: it ended on `stop=safety_budget`, `status=ok_no_change_inconclusive`, `valid_s=119.608` against a 120 s target, because the 5 s pause sits inside the 180 s safety budget. It answers ONE question: after stage A put CONTROL in the running shape, the probe waited 5.00000022 s with PI masked, no handler and nothing in flight — CONTROL `8c`→`8c`, IRQ `0500`→`0500`, INTSR `00012000`→`00012000`, INTMR `000001fa`→`000001fa` — then installed the handler, unmasked, and ran 1108063 unmasks = deliveries = acks = rearms with a clean restore. 10446 frames, 38 diagnostics (38 SOURCE_SERVICED, 38 disc-extra, 0 quarantined, follow-up 38/38). Same prefix-only script rule as the other vstate runs. **The claim is the duration and the position exercised, nothing more.** GBP-HW-116…119 |
| `hw-gamecube-gbp-2026-09-18-color-0001.gbpreplay` + `hw-gamecube-gbp-2026-09-18-color-0001-color.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-VIDEO-003 build color-0001 commit 9d8302d, DOL sha256 `cc88e4c4…7cf4`, log sha256 `28c5a06c…bb8c` (40790 B), sidecar sha256 `95595f9d…2bb7` (461684 B, `OGBPCOL1` **v1**, byte-identical copy of the console's file) | hardware capture, **the first colour run and the first run with a Game Pak** (`CONTROL orig=92`, presence bit `0x02`). The runtime reached its target — `stop=color_certified`, 5000 ms pre-handler wait elapsed in 5.00000020 s, frames `not_complete / resync / eligible ×3`, three certified with identical `sig[40]`, 440 deliveries = acks = rearms = unmasks, 0 errors, 0 R3 disagreements, clean restore — and **the analyser refused it**: the three certified frames are not byte-identical over the full 153600-byte raw frame (`VERDICT: INCONCLUSIVE - CERTIFIED RAW MISMATCH`, first difference at `0x108`, A/B 2125 B/C 2073 A/C 2137). Every difference is in byte 0 or 2 of its group and **none** in bytes 1 or 3, so the consumed projection `(b1<<8)|b3` is identical in 38400 of 38400 words. Read through that projection the eight bars are the outer-group swap of the stimulus on 8/8 bars — recorded as a **post-gate diagnostic projection**, never as the mapping: **U-GBP-011 stays OPEN**. Same prefix-only script rule as the vstate runs (the 440 service cycles kept no per-delivery log record); the sidecar is the evidence. Both CRCs verify (`96002fc9` / `19ebfbda`). Pinned by `tests/host/test_vcolor.py::PhysicalColor0001`. GBP-HW-120…125 |
| `hw-gamecube-gbp-2026-09-18-color-0002.gbpreplay` + `hw-gamecube-gbp-2026-09-18-color-0002-color.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-VIDEO-003 build color-0002 commit 39f1980, DOL sha256 `d3c1f09e…f29c1` (built CLEAN at that exact commit before the run, no `-dirty`), log sha256 `194f92f9…8ea1` (40900 B), sidecar sha256 `f49c4cf2…8fd0` (461684 B, `OGBPCOL1` **v1**, byte-identical copy of the console's file) | hardware capture, **the confirmatory colour run**. Judged by a contract written BEFORE it (`HARDWARE_TESTS.md` §V4, commit `a86b079`) with an analyser that already existed at that commit (`tools/vcolor2.py`), neither touched afterwards. `STANDING: CONFIRMATORY`, `VERDICT: CONFIRMED_EXACT_H1_OUTER_GROUP_SWAP` — the consumed-word gate passed 38400/38400, `FLAG15_STABLE` with one flag per frame at (0,0), all eight bars uniform, and exactly one of the seven pre-registered transformations reproducing all eight values. Runtime: `stop=color_certified`, 440 deliveries = acks = rearms = unmasks, 0 errors, 0 R3 disagreements, clean restore, and **`truncated=0`** — the first physical proof that splitting `PREHANDLERWAIT` fixed the logger defect. Full raw still differs (2434 / 2483 / 2485 bytes) **entirely in bytes 0 and 2, never 1 or 3**, reported as the mandatory diagnostic for U-GBP-029. Cross-run: its three certified frames are byte-identical to `color-0001`'s in the consumed projection, 38400/38400 in all three pairs. Same prefix-only script rule as the other runs; the sidecar is the evidence. Both CRCs verify (`f834e435` / `f3f85d64`). **Closed U-GBP-011.** GBP-HW-127…133 |
| `hw-gamecube-gbp-2026-09-14-probe-0001.gbpreplay` | **SOURCE = physical GameCube + Game Boy Player**, GBP-PROBE-001 build probe-0001 commit 55ed6c1, derived from the device log sha256 `98ba20d5…f014` (kept unmodified under `captures/local/`) | hardware capture, sanitized (records only; header/setup in HARDWARE_TESTS.md) |
| `hw-gamecube-gbp-2026-09-19-idxcap-run1-qual.bin` | **DERIVED, not a capture.** The structural PROJECTION (`tools/vqual.py`, format `OGBPQUAL1`, 24652 B, sha256 `ae867b00…`) of the GBP-VIDEO-004 run-1 witness sidecar `6c822d63…` (8946060 B), whose raw log is `165a3e32…` (66427 B) | §V5.44.6 replay input. Keeps **only** `frame_index`, `blocks`, `flags`, `completeness` — the assembler's verdict on frame SHAPE. It carries **no witness words**, so no `FRAME_ID`, `STATUS`, `SYNC`, `CRC-8` or pixel can reach the code under test even by accident. Provenance (source size + SHA-256) is inside the file header, so it is re-derivable and checkable against the raw evidence |
| `hw-gamecube-gbp-2026-09-19-idxcap-run2-qual.bin` | same projection of the run-2 sidecar `111ea227…` (8946060 B), log `e02d1160…` (66515 B); fixture sha256 `9843248e…` | as above. Run 2 is the **systemic-duplication control**: no window of any size changes its `OBSERVED_DISCONTINUITY` |
| `hw-gamecube-gbp-2026-09-19-idxcap-run3-qual.bin` | same projection of the run-3 sidecar `6eb7585c…` (8946060 B), log `1c8e2aaa…` (66356 B); fixture sha256 `42f34565…` | as above. Run 3 is the **startup-only case**: the window turns its counterfactual population contiguous, while the capture's own recorded verdict stays `OBSERVED_DISCONTINUITY` for ever |
| `hw-gamecube-gbp-2026-09-19-idxcap-run4-qual.bin` | same projection of the **run-4** sidecar `91feed16…` (8946060 B), log `299294ba…` (62513 B); fixture sha256 `ba061356…` | **Different in kind from runs 1-3.** Those are replays -- the window applied offline to captures taken without it. Run 4's window ran ONLINE on hardware, so this fixture is not a counterfactual: it is the shape of the population the runtime actually retained (2048 records, `frame_index` 70..2117, all COMPLETE_40, zero structural anomalies). It is the CI-sized regression guard for GBP-HW-184; the analyzer verdict itself needs the 8.9 MB capture and is checked only where that exists |
| `hw-gamecube-gbp-2026-09-19-idxcap-run5-qual.bin` | same projection of the **run-5** witness sidecar `853c62a3…` (8946060 B), log `73def124…` (66838 B); fixture sha256 `68567864…` | the CI-sized structural record of run 5's retained population: 2048 records, `frame_index` 70..2117, all COMPLETE_40 |
| `hw-gamecube-gbp-2026-09-19-stream-0007-run5-disp.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-VIDEO-004 build `stream-0007` commit `ddf8db6`, DOL `74b74886…`, log `73def124…` (66838 B). A **byte-identical copy of the console's own `OGBPDISP1` file** (287772 B, sha256 `8bf23585…`) | the FIRST physical downstream disposition trace: 2114 lifecycles, 2114 decisions, 19 `HOLD_PREVIOUS`, header CRC `443dd4d6`, global CRC `b90822f4`. Small enough to version whole, so GBP-HW-194…200 are recomputed by `tests/host/test_disp_run5.py` on every suite run. The 8.9 MB witness sidecar it joins to is NOT versioned, so the join tests skip where that capture is absent |
| `hw-gamecube-gbp-2026-09-19-stream-0008-run6-disp.bin` | **SOURCE = physical GameCube, GBP_PRESENT=yes**, GBP-VIDEO-004 build `stream-0008` commit `5126a19`, DOL `a9efe181…`. Byte-identical copy of the console's `OGBPDISP2` (357 524 B, sha256 `36b68461…`) | run 6, the first Policy-A run (§V5.50, GBP-HW-202…212): 2047/2047 handed off, 7 display repeats. `tests/host/test_disp_run6.py` |
| `hw-gamecube-gbp-2026-09-19-idxcap-run6-qual.bin` | projection of the run-6 witness `311a26a7…` | as the run-1…5 projections |
| `hw-gamecube-gbp-2026-09-19-stream-0009-run7-disp.bin` | build `stream-0009` commit `59d2f57`, DOL `4d0337bb…`; `OGBPDISP2` 379 340 B, sha256 `edf38428…` | run 7, first NORMAL startup (§V5.53): window opened 1.00 s before the stimulus. `tests/host/test_disp_run7.py` |
| `hw-gamecube-gbp-2026-09-19-idxcap-run7-qual.bin` / `…-run7-struct.json` | projection of the run-7 witness `1e502bc8…`; the struct file adds per-record canonical validity, FRAME_ID, STATUS and the frozen verdict WITH its composition (`intact 1988 / INVALID 60`) | Conclusion II of §V5.53 recomputed without the 8.9 MB witness |
| `hw-gamecube-gbp-2026-09-20-stream-0009-run8-disp.bin` | RUN B / GBP-VIDEO-005, retail cartridge, same `stream-0009` DOL; `OGBPDISP2` 379 300 B, sha256 `2edadb49…` | §V5.54.11; operator observations live in the docs, never in fixture bytes. `tests/host/test_disp_run8.py` |
| `hw-gamecube-gbp-2026-09-20-idxcap-run8-qual.bin` | content-blind projection of the RETAIL witness `d93d7781…` — no strip decoded, no OGBPIDX verdict | structural shape only |
| `hw-gamecube-gbp-2026-09-20-stream-0010-run9-disp.bin` | build `stream-0010` commit `fbaea00`, DOL `6b57d669…`; `OGBPDISP2` 401 796 B, sha256 `7a4b032a…` | run 9, the not-before gate on hardware (§V5.56, GBP-HW-231…238). `tests/host/test_disp_run9.py` |
| `hw-gamecube-gbp-2026-09-20-idxcap-run9-qual.bin` / `…-run9-struct.json` | projection of the run-9 witness `247bae66…` (8 946 060 B); the struct file carries run, build, commit, DOL/raw sizes and hashes, topology (BBA DISCONNECTED), tool identities, the verdict WITH composition (`intact 2048 / INVALID 0`), WITELIG/WITQUAL/STARTUP fields and the truncation finding with its derivation | the whole run-9 claim recomputed from a fresh clone without the private log |
| `hw-gamecube-gbp-2026-09-20-stream-0010-run10-disp.bin` | **GBP-BBA-001 / RUN 10**, the exact run-9 binary (`stream-0010` `fbaea00`, DOL `6b57d669…`) with the BBA physically PRESENT and Ethernet DISCONNECTED (operator declaration); `OGBPDISP2` 401 796 B, sha256 `53c67572…` | §V5.57.14, GBP-HW-239…243: paired topology control against run 9, no detected regression. `tests/host/test_disp_run10.py` |
| `hw-gamecube-gbp-2026-09-20-idxcap-run10-qual.bin` / `…-run10-struct.json` | projection of the run-10 witness `1cba0fd7…` (8 946 060 B); the struct file carries the operator's topology declaration AS a declaration, the supplied `-bba` drop names, the canonical archive paths, every hash, the verdict WITH composition (`intact 2048 / INVALID 0`), the gate/startup fields, the expected `WITELIG` truncation, and the run-9 comparison with a note that equality is an observation | the whole run-10 claim recomputed from a fresh clone |
| `hw-gamecube-gbp-2026-09-20-stream-0011-run11-disp.bin` | **GBP-VIDEO-006 / RUN 11**, build `stream-0011` commit `97c78c2`, DOL `df2873ee…` — the `WITELIG` reporting repair (GBP-VID-033) on run 10's topology, BBA physically PRESENT and Ethernet DISCONNECTED (operator declaration); `OGBPDISP2` 401 956 B, sha256 `04feeca9…`, byte-identical copy of the console's file | §V5.58.9, GBP-HW-244…249: GBP-VID-033 physically validated for this controlled run, no detected regression. `tests/host/test_disp_run11.py` |
| `hw-gamecube-gbp-2026-09-20-idxcap-run11-qual.bin` / `…-run11-struct.json` | projection of the run-11 witness `9b62415b…` (8 946 060 B); the struct file carries the operator's topology declaration AS a declaration, the console's generated drop names, the canonical archive paths, every hash, the verdict WITH composition (`intact 2048 / INVALID 0`), the log header (`lines=651 dropped=0 truncated=0`), both reporting records with their field lists and payload widths (167 / 98 of 248), the direct `qual_streak_at_eligible=0` with its counter cross-check, transport, Policy A, and the run-10 comparison with a note that numerical closeness is an observation | the whole run-11 claim recomputed from a fresh clone |
| `hw-gamecube-gbp-2026-09-20-stream-0013-run12-disp.bin` | **GBP-VIDEO-007 / GBP-VIDEO-008 / RUN 12**, build `stream-0013` commit `7d7a6d8`, DOL `5391c3fe…`, stimulus `coord-0001` delivery `a769cc11…` (re-flashed), BBA PRESENT / Ethernet DISCONNECTED and the composite → RCA-to-HDMI converter → HYDIS HV150UX2 display chain (operator declaration); `OGBPDISP2` 401 356 B, sha256 `91c2f805…`, byte-identical copy of the console's file | §V6.20, GBP-HW-250…255: both experiments INCONCLUSIVE (the shared source gate failed, GBP-VID-034); Policy A / transport / startup clean. `tests/host/test_run12.py` |
| `hw-gamecube-gbp-2026-09-20-stream-0013-run12-full.bin` | **RUN 12 `OGBPFULL1 v1`** 1 844 492 B, sha256 `fb09a777…`, byte-identical copy of the console's file: K = 8 content-blind full-frame samples (raw + texture of one lifecycle each) at `frame_index` 356 + 256·i | §V6.20.6, GBP-HW-252: the frozen `tools/vfull.py` dependent-variable analysis PASS 8/8 with 0 mismatches — SUBORDINATE evidence; GBP-VIDEO-008 stays INCONCLUSIVE. Recomputed from a clone by `tests/host/test_run12.py` |
| `hw-gamecube-gbp-2026-09-20-stream-0013-run12-vi.bin` | **RUN 12 `OGBPVI1 v1`** 152 396 B, sha256 `d301e96e…`, byte-identical copy of the console's file: 2377 hand-over records, 2370 latched with the VI base registers read back, 6 superseded | §V6.20.7, GBP-HW-255, GBP-VID-035: the `tools/vvi.py` frozen at the run read 0/2370 consistent registers (frozen L_k = 0, kept as history); the defect — a 24-bit mask before the flag shift — was repaired in software by Issue #11 (§V6.21): the corrected post-run replay reads 2370/2370 top and bottom and derives L = 40/40/38/40 (two R_3 hand-overs SUPERSEDED, no latch record); verdicts unchanged. `tests/host/test_run12.py` |
| `hw-gamecube-gbp-2026-09-20-idxcap-run12-qual.bin` / `…-run12-struct.json` | projection of the run-12 witness `fe1c1c0a…` (8 946 060 B); the struct file carries the operator's topology declaration and display chain AS declarations, the literal visual report (1 2 3 4), the console's generated drop names, the canonical archive paths, every hash, the frozen verdict WITH composition (`OBSERVED_DISCONTINUITY`, intact 2048 / INVALID 0, the two duplicates at `frame_index` 761→762 and 2202→2203), the per-record frozen decode (2048 records with FRAME_ID and STATUS), the frozen vfull / vdisp / vvi results, the raw VI cross-check, both formal verdicts, the two findings and the run-11 comparison | the whole run-12 claim — and its two INCONCLUSIVE verdicts — recomputed from a fresh clone without the private log |
| `hw-gamecube-gbp-2026-09-21-stream-0013-run13-disp.bin` | **GBP-VIDEO-007 / GBP-VIDEO-008 / RUN 13**, build `stream-0013` commit `7d7a6d8`, DOL `5391c3fe…` (the RUN 12 image, not rebuilt), stimulus `coord-0002` delivery `276ad987…` (flashed), BBA PRESENT / Ethernet DISCONNECTED and the composite → RCA-to-HDMI converter → HYDIS HV150UX2 display chain (operator declaration); `OGBPDISP2` 401 396 B, sha256 `c75e986a…`, byte-identical copy of the console's file | §V6.25, GBP-HW-256…260: GBP-VIDEO-007 = PASS (CLAIM-D only) · GBP-VIDEO-008 = PASS (CLAIM-A/B, eight sampled frames); the shared source gate passed (`OBSERVED_CONTIGUOUS`); Policy A / transport / startup clean. `tests/host/test_run13.py` |
| `hw-gamecube-gbp-2026-09-21-stream-0013-run13-full.bin` | **RUN 13 `OGBPFULL1 v1`** 1 844 492 B, sha256 `cb884e27…`, byte-identical copy of the console's file: K = 8 content-blind full-frame samples (raw + texture of one lifecycle each) at `frame_index` 356 + 256·i | §V6.25.6, GBP-HW-258: the unchanged `tools/vfull.py` analysis PASS 8/8 with 0 mismatches and — the run being admissible — the formal GBP-VIDEO-008 = PASS for those eight frames only. Recomputed from a clone by `tests/host/test_run13.py` |
| `hw-gamecube-gbp-2026-09-21-stream-0013-run13-vi.bin` | **RUN 13 `OGBPVI1 v1`** 152 396 B, sha256 `2a772ae0…`, byte-identical copy of the console's file: 2377 hand-over records, 2371 latched with the VI base registers read back, 5 superseded | §V6.25.7, GBP-HW-260: the corrected `tools/vvi.py` (§V6.21, used prospectively) reads 2371/2371 top and bottom and derives L = 40/40/39/40 (one R_3 hand-over SUPERSEDED, `frame_index` 1754, no latch record: instrumentation semantics only); beside the operator's 1 2 3 4 → GBP-VIDEO-007 = PASS as CLAIM-D. `tests/host/test_run13.py` |
| `hw-gamecube-gbp-2026-09-21-idxcap-run13-qual.bin` / `…-run13-struct.json` | projection of the run-13 witness `dcbcfd3e…` (8 946 060 B); the struct file carries the operator's topology declaration and display chain AS declarations, the literal visual report (1 2 3 4, ~8 s), the console's generated names and how the files arrived (moved into `logs/`, overwriting RUN 12's bare-name copies; RUN 12's archive verified intact), the canonical archive paths, every hash, the frozen verdict WITH composition (`OBSERVED_CONTIGUOUS`, intact 2048 / INVALID 0, 2046/2046 +1, the four entries +1), the per-record frozen decode (2048 records with FRAME_ID and STATUS), the vfull / vdisp / corrected-vvi results, the register model, both formal verdicts with their boundaries and the RUN 12 comparison | the whole run-13 claim — and its two PASS verdicts inside their boundaries — recomputed from a fresh clone without the private log |
| `hw-gamecube-gbp-2026-09-21-stream-0014-run14-disp.bin` | **GBP-INPUT-001 / RUN 14** (the first physical KEYPAD write; the Enhanced Control Checker's counted walk A), build `stream-0014` commit `0ff8355`, DOL `ef76a170…` (Issue #19's candidate, not rebuilt), the checker `76924c13…` on the EZ-Flash NOR (the Operator's media; nothing from it here), the same GameCube, BBA PRESENT / Ethernet DISCONNECTED ("BBA conectado sem cabo de rede"), the composite → RCA-to-HDMI converter → HYDIS HV150UX2 chain unchanged and a GENERIC third-party controller (the Operator's post-run declaration, Issue #25; the same Game Boy Player by his declared inventory: one console, one GBP); `OGBPDISP2` 400 396 B, sha256 `82bc7434…`, byte-identical copy of the console's file | §V7.2, GBP-HW-261…265: Question M = PASS · Question O = AS-ASSIGNED; INPUT 7 892 / 7 892 writes, failed 0, 42 key changes; Policy A / transport / startup clean; `truncated=1` = the ENVINPUT clip (GBP-KEY-008). `tests/host/test_run14.py` |
| `hw-gamecube-gbp-2026-09-21-stream-0014-run14-full.bin` | **RUN 14 `OGBPFULL1 v1`** 1 844 492 B, sha256 `30c144d7…`, byte-identical copy of the console's file: K = 8 content-blind full-frame samples at `frame_index` 356 + 256·i — **the checker's tally screen, the machine-decodable record of the end state (FACT as data)** | §V7.2.6, GBP-HW-264: under the unmodified `tools/vfull.py` parser the tallies decode by exact glyph match to `1 2 · · · · 6 5 3 4` from s3 on (= the walk's expectation and the Operator's vector); the tool's own verdict INCONCLUSIVE (its oracle is OGBPCOORD1) recorded, not judged. Re-decoded from a clone by `tests/host/test_run14.py` |
| `hw-gamecube-gbp-2026-09-21-stream-0014-run14-vi.bin` | **RUN 14 `OGBPVI1 v1`** 152 396 B, sha256 `ef0a1261…`, byte-identical copy of the console's file: 2377 hand-over records, 2372 latched, 4 superseded | §V7.2.9: the corrected `tools/vvi.py` reads 2372/2372 top and bottom; recorded beside RUN 13's, not a gate of GBP-INPUT-001. `tests/host/test_run14.py` |
| `hw-gamecube-gbp-2026-09-21-idxcap-run14-qual.bin` / `…-run14-struct.json` | projection of the run-14 witness `d42ebb1a…` (8 946 060 B); the struct file carries every identity, the walk and its arithmetic expectation, the declarations as relayed and what was NOT posted, the Operator's literal vector, the receipt (RUN 14 in `logs/` bare names), the summary records VERBATIM (so the INPUT machine gate is recomputable from the tree), the ENVINPUT clip's derivation, the tools' results, the per-sample tally readings with the glyph table, the register model, both verdicts with their boundaries, the uniform per-record decode stored once and the RUN 13 comparison | the whole run-14 claim — M = PASS, O = AS-ASSIGNED inside their boundaries, the routing CORROBORATED — recomputed from a fresh clone without the private log |
| `hw-gamecube-gbp-2026-09-21-stream-0014-run15-disp.bin` | **GBP-INPUT-001 / RUN 15** (walk B: L 1, R 2, UP 3, DOWN 4, LEFT 5, RIGHT 6), the same image, instrument and declared topology as RUN 14; `OGBPDISP2` 400 436 B, sha256 `7577a72b…`, byte-identical | §V7.2, GBP-HW-261…265: M = PASS · O = AS-ASSIGNED; INPUT 7 895 / 7 895, failed 0, 42 key changes; the RUN 15 drop arrived in `logs/run15/`. `tests/host/test_run14.py` |
| `hw-gamecube-gbp-2026-09-21-stream-0014-run15-full.bin` | **RUN 15 `OGBPFULL1 v1`** 1 844 492 B, sha256 `0c3e5612…`, byte-identical: the checker's tally screen in all eight samples | §V7.2.6, GBP-HW-264: decodes to `1 2 3 4 5 6 · · · ·` from s3 on (= the walk's expectation and the Operator's vector). `tests/host/test_run14.py` |
| `hw-gamecube-gbp-2026-09-21-stream-0014-run15-vi.bin` | **RUN 15 `OGBPVI1 v1`** 152 396 B, sha256 `fb6d12a7…`, byte-identical: 2377 handed, 2373 latched, 3 superseded | §V7.2.9: corrected `tools/vvi.py` 2373/2373; `frame_index` 1169 latched two retraces after its hand-over (instrumentation semantics, recorded). `tests/host/test_run14.py` |
| `hw-gamecube-gbp-2026-09-21-idxcap-run15-qual.bin` / `…-run15-struct.json` | projection of the run-15 witness `be1785ce…` (8 946 060 B); the struct file as run14's, for walk B | as run14's |
| `stimulus-coord-0001-canonical.gba` | **the frozen `coord-0001` stimulus, byte for byte**: the canonical ROM that ran in RUN 12 (through its delivery image), 3 496 B, sha256 `90343b64…0a1f`; a copy of the reproducible build output (`make stimulus-coord`), NOT a capture and NOT a new identity | §V6.22, GBP-VID-034: `tools/coordtime.py analyse` executes its exact IWRAM code in a cycle model; `tests/host/test_coordtime.py` pins the model against it so the root-cause analysis recomputes from a clone |
| `stimulus-coord-0002-canonical.gba` | **the `coord-0002` canonical ROM, byte for byte**: the timing-safe second implementation of OGBPCOORD1 (§V6.23, Issue #13), 3 620 B, sha256 `319dacb7…093f`; a copy of the deterministic build output (`make stimulus-coord2`), NOT a capture, NOT physically executed, no run reserved | `tools/coordtime.py analyse` (profile coord-0002) proves every entry PREPARE 176 863 cycles below the budget; `tests/host/test_coordtime2.py` pins it; `tests/host/test_agb_coord2.py` proves the picture equals `tools/icoord.py` and coord-0001's |

`.gbpreplay` files are scripts for `src/gbp/gbp_replay.c` (format in its
header), generated by `tools/probelog.py fixture <log>` with the metadata
header prepended; the raw device logs stay unmodified under
`captures/local/` (ignored) and are identified by SHA-256 in the header.

Whole-block reads (GBP-AV-SERVICE-001, executed 2026-09-16) appear in a
script as `B <addr> <len> <rc> [<crc32>]` lines that carry no bytes: the
bytes of a run live in its block sidecar `<TestID>_<BuildID>-blocks.bin`
(format: `src/gbp/gbp_avdump.h`; parser: `tools/avdump.py`), which the
physical fixture keeps next to the script as `<fixture>-blocks.bin`
(`# BLOCKS=`, `# BLOCKS_SHA256=`, `# BLOCKS_SIZE=` in the script's header);
the replay verifies every block it is given against the script's CRC-32
and reports missing blocks instead of inventing them (the probe's buffers
then hold its pre-fill, never physical bytes, and the run exits 1). The
physical GBP-AV-SERVICE-001 fixture above replays end to end with its
sidecar (`tests/host/test_avsvc_replay.py`, `tests/unit/test_gbp_avsvc.c`,
`tests/host/test_hw_fixture.py`, `tests/host/test_avdump.py`); the same
test file also round-trips a synthetic log and sidecar under `build/` and
drives the probe with the physical 003B and 004 fixtures cut before their
device ACK (the delivery and the PRESVC reads are physical; the drain meets
a transport without whole-block reads — nothing after a physical record is
invented). GBP-VIDEO-001 (**physically executed 2026-09-16**) uses a
second, independent sidecar format for a whole bounded capture: `OGBPSEQ1`
(`<TestID>_<BuildID>-seq.bin`, format 1 — the cycle table, the VIDEO and AUDIO
tables and the raw blocks; layout `src/gbp/gbp_avseqdump.h`, parser
`tools/avseq.py`). It does not replace or change the `OGBPBLK1` block sidecar
of GBP-AV-SERVICE-001, which stays at format 2 and byte-identical. The physical
GBP-VIDEO-001 fixture and its sidecar are listed above;
`tests/host/test_video_replay.py` pins their hashes and asserts that no AUDIO
payload was invented for the 135 drains that preserved none. The host round trip
keeps its synthetic log, fixture and sidecar under `build/` only. The physical GBP-AV-SERVICE-001
fixture above doubles as the exact prefix of that experiment's first cycle —
that run is one cycle of its repeated service — and replays through it end to
end with 0 mismatches.

The raw AUDIO / VIDEO bytes of the sidecar are evidence,
recorded and not interpreted (EVIDENCE.md GBP-HW-057/058).

GBP-VIDEO-002 extends the same sidecar family, one frozen version per physical
build: `OGBPSEQ1` **v2** (`vstate-0001`), **v3** (`vstate-0002`, adds the 96-byte
semantic-disagreement diagnostic) and **v4** (`vstate-0003`, adds the 1 024-byte
semantic block and the 160-byte diagnostic array) and **v5** (`vstate-0004`,
**physically executed 2026-09-17**: v4's layout byte for byte, under a producer
contract the parser enforces with cross-field invariants). Each
version is dispatched strictly and every file is judged by its own version's
rules; no file is ever read under another version's contract. The physical v4 file above carries a
**known producer defect** (EVIDENCE.md GBP-HW-104): a diagnostic record's
current-cycle fields may have been overwritten by a later service cycle. The
format and its parser stay **frozen and unchanged** — adding a cross-field rule
now would make the parser refuse the only physical v4 file in existence, which
would destroy evidence rather than protect it. Instead `tools/vstate.py diag`
prints a non-fatal **PRODUCER WARNINGS** section (`ack_after_next_cause`,
`rearm_after_next_cause`, `authority_not_majority`), so the defect is detectable
offline from the file alone: 23, 23 and 22 warnings respectively on this file.
`tests/host/test_vstate.py::PhysicalV4` pins the file's hash, its contiguous
layout, all 23 disagreements recomputed from their raw 32 bytes, and the
detectability of the defect itself.

The GBP-INIT-003A fixture above is physical. The same tooling path is also
exercised by `tests/host/test_initirqa_replay.py` on a script generated
from the host mock (marked `# SYNTHETIC` by `--note`) and kept under
`build/`; synthetic scripts are never placed in this directory.
`tests/host/test_initirqb_replay.py` round-trips a mock log of the delivery
stage the same way (its `I u` line carries the extended handler record, the
POSTACK main-loop W1C becomes `P a`), replays the physical 003A fixture
through the 003B probe (it is the real prefix up to the EVENT and stops at
the handler install because that run had no interrupt path) and replays the
physical 003B fixture end to end. The 003B fixture's source log carries
`TEARDOWN start … pi_policy=never_unmasked`, a label defect of build
initirqb-0001 (the shared 003A teardown printed its own fixed policy in a
run that had unmasked once; the same log's `RESTOREB … unmasked=1
masked_again=1` is the primary record); the log and the fixture are kept as
written, later builds print `pi_policy=unmasked_once`, and the replay through
the corrected probe reports that label. The GBP-INIT-004 fixture (2026-09-16)
replays the whole physical run to its real result: one delivery and one ACK,
then `anomaly_source_not_cleared` at POSTACK-0 with no re-arm written — it
carries `I p 0` (the generation publication) and the multi-cycle handler's
record, and no evidence whatsoever about `IRQ := 0` after an ACK or about a
second delivery. `tests/host/test_initirq4_replay.py` also round-trips a
synthetic three-cycle mock log (with the optional `I p <gen>` lines) under
`build/` and drives the 004 probe with the physical 003B fixture cut before
its CONTROL restore — cycle 0 verbatim up to the POSTACK, then the first
re-arm meets an exhausted script; nothing after a physical record is ever
invented. The grammar
has an optional `P p <intsr>` line (the value the next `poll_intsr`
returns) for the INTSR-polling loops of that probe; the polling loops
replay with one time-base read per sample, so poll counters differ from
the device run while every other value is verbatim. The source log of the
003A fixture carries `WINDOW tag=A1 intsr13_seen=1`, a formatter defect of
build initirqa-0001 (the global flag was printed in the A1 record); the
primary records show INTSR bit 13 = 0 throughout A1, the fixture keeps the
log's chronology, and the corrected probe reports `intsr13_in_phase=0`
when it replays it.
